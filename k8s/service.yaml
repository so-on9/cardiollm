apiVersion: v1
kind: Service
metadata:
  name: cardiollm-proxy
  namespace: cardiollm
spec:
  type: ClusterIP
  selector:
    app: cardiollm-proxy
  ports:
    - name: http
      port: 80
      targetPort: 8000
